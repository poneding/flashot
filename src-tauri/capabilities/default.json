{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "overlay-*", "settings", "about"],
  "permissions": [
    "core:default",
    "shell:default",
    "updater:default",
    "process:default"
  ]
}
